pipeline {
   agent any

   stages {
      stage('clone repository') {
         steps {
            echo "hello"
         }
      }
      stage('adding files') {
          steps {
              echo "welcome"    
              }
      }
      stage('commit') {
          steps {
              echo "end"
          }
      }
   }
}
